# CibESphere Environment Configuration
# Copia este archivo como .env y completa las variables necesarias

# ====================================
# API Configuration
# ====================================
# URL del backend Go (opcional - usar localhost para desarrollo)
VITE_API_BASE_URL=http://localhost:8080

# ====================================
# Map Configuration 
# ====================================
# Configuración del mapa por defecto - centrado en España
VITE_MAP_DEFAULT_LAT=40.4165    # Latitud central (Madrid)
VITE_MAP_DEFAULT_LNG=-3.70256   # Longitud central (Madrid)
VITE_MAP_DEFAULT_ZOOM=6         # Nivel de zoom inicial

# ====================================
# Development Configuration
# ====================================
# Puerto del servidor de desarrollo (debe ser 5000 para Replit)
PORT=5000

# Modo de desarrollo
NODE_ENV=development

# ====================================
# Backend Integration (Opcional)
# ====================================
# Las siguientes variables son opcionales si tienes el backend funcionando:

# Base de datos PostgreSQL
DATABASE_URL=postgresql://user:password@localhost:5432/cibesphere_dev
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=cibesphere_user
POSTGRES_PASSWORD=your_secure_password
POSTGRES_DB=cibesphere_dev

# Redis para sesiones/cache
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_here
JWT_EXPIRES_IN=7d

# File Upload Configuration
UPLOAD_MAX_SIZE=2097152  # 2MB en bytes
UPLOAD_ALLOWED_TYPES=image/jpeg,image/png,image/gif,image/webp

# Email Configuration (para notificaciones)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# ====================================
# Production Only
# ====================================
# Solo necesarias en producción:
CORS_ORIGINS=https://your-domain.com,https://www.your-domain.com
SECURE_COOKIES=true
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW_MS=900000  # 15 minutos